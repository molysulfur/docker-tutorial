sudo: required
services:
    - docker

before_install: 
    - docker build -t molysulfur/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true molysulfur/docker-react npm run test -- --coverage